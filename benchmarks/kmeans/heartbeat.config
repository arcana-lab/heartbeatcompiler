CHUNKSIZE ?= 64
MACROS_HEARTBEAT += -DCHUNKSIZE=$(CHUNKSIZE)
PASS_ARGUMENTS_HEARTBEAT_COMPILER += -chunksize $(CHUNKSIZE)

ARRAY_UPDATE_MAP ?= false
ifeq ($(ARRAY_UPDATE_MAP), false)
  MACROS_HEARTBEAT += -DARRAY_PRIVATIZATION
else
  MACROS_HEARTBEAT += -DARRAY_UPDATE_MAP
endif
